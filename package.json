{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --parallel --scope @webby/client --scope @webby/server"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.0.2"
  }
}
